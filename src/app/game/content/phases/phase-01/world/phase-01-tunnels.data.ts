import { TunnelData } from '../../../../domain/world/phase-playable-data.model';

/**
 * Túneis removidos.
 * Como a passagem "por dentro" no estilo Metroid não ficou correta,
 * é melhor desativar completamente por agora do que manter algo bugado.
 */
export function getPhase01Tunnels(): TunnelData[] {
  return [];
}
